drupal:
    settings:
        databases:
            default:
                default:
                    database: 
                    username: 
                    password:
                    driver: mysql
                    host: localhost
    include:
        require_once: [%DRUPAL_ROOT . '/sites/default/vendor/autoload.php']
